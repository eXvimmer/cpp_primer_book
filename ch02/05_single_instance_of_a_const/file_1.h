extern const int bufSize;
// NOTE: extern signifies that bufSize is not local to this file and that it's
// definition will occur elsewhere.

// NOTE: to share a const object among multiple files, you must define the
// variable as extern
